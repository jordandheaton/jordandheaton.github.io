# Secrets -- never commit your Pinecone API key
.env

# Advisor spend/quota state (per-deployment runtime data, not source)
advisor_usage.json
advisor_usage.tmp

# Python
.venv/
__pycache__/
*.pyc

# Large generated data (optional: comment out if you want catalog.json tracked)
# data/catalog.json
