# --- scraper.py ---
requests>=2.31.0
urllib3>=2.0.0

# --- embed_and_load.py ---
# Local, free embedding engine (BAAI/bge-small-en-v1.5 -> 384-dim vectors).
sentence-transformers>=2.7.0
# Modern Pinecone SDK. 'pinecone' is the current package name; it supersedes the
# older 'pinecone-client'. Either installs the same `from pinecone import Pinecone`
# API -- do NOT install both in the same environment.
pinecone>=5.0.0
# Loads PINECONE_API_KEY / ANTHROPIC_API_KEY from a local .env file.
python-dotenv>=1.0.0

# --- ask_advisor.py ---
# Claude API client for the RAG generation step (grounded answers).
anthropic>=0.40.0

# --- sources/ (website scrapers beyond the Coursedog catalog) ---
# HTML parsing for regular BYU webpages (study abroad, certificates, ...).
beautifulsoup4>=4.12.0
# Some certificate pages link out to PDFs; extract their text.
pypdf>=4.0.0
flask>=3.0.0
